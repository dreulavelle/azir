name: azir

# Two services. Azir is one binary — embedded NATS with JetStream, the API, the
# frontend, and supervised plugins — and Postgres is the one external
# dependency, because semantic recall needs a real ANN index and ingest is
# genuinely concurrent.
services:
  azir:
    build:
      context: ..
      dockerfile: Dockerfile
    image: azir:local
    environment:
      DATABASE_URL: postgres://azir:${POSTGRES_PASSWORD:-azir_dev_only}@postgres:5432/azir?sslmode=disable
      # Development key only. Generate a real one with `make keygen` and pass
      # it as a Docker secret or platform secret anywhere that will hold a
      # customer's credentials.
      AZIR_MASTER_KEY: ${AZIR_MASTER_KEY:-YXppci1kZXZlbG9wbWVudC1rZXktbm90LXNlY3JldCE=}
      AZIR_LOG_LEVEL: ${AZIR_LOG_LEVEL:-info}
      # Set NATS_URL to point at an external cluster instead of the embedded
      # server. Everything else is unchanged when you do.
    ports:
      - "${AZIR_PORT:-8080}:8080"
    volumes:
      # JetStream's store. Postgres holds everything else.
      - azir-data:/var/lib/azir
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  postgres:
    # pgvector is why this is Postgres; the image ships the extension so the
    # migration can simply CREATE EXTENSION.
    image: pgvector/pgvector:pg18
    environment:
      POSTGRES_USER: azir
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-azir_dev_only}
      POSTGRES_DB: azir
    command:
      - "postgres"
      - "-c"
      - "config_file=/etc/postgresql/postgresql.conf"
    volumes:
      # Mounted at /var/lib/postgresql rather than .../data, which is what the
      # 18+ images expect: the cluster then lives in a version-named
      # subdirectory, so a future major upgrade can use `pg_upgrade --link`
      # without both versions straddling a mount boundary.
      - pg-data:/var/lib/postgresql
      - ./postgres/postgresql.conf:/etc/postgresql/postgresql.conf:ro
    ports:
      # Published for psql during development, off 5432 so a Postgres already
      # running on the host does not collide.
      - "${AZIR_PG_PORT:-5433}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U azir -d azir"]
      interval: 5s
      timeout: 3s
      retries: 12
    restart: unless-stopped
    # Asynchronous I/O is already on via io_method=worker. Switching to
    # io_uring needs the syscalls Docker's default seccomp profile blocks:
    #
    #   security_opt:
    #     - seccomp=./postgres/seccomp-io-uring.json
    #
    # and io_method=io_uring in postgresql.conf. Left off deliberately —
    # io_uring bypasses seccomp filtering rather than merely widening it, and
    # this container holds every customer's PBX credentials. See the tuning
    # notes in README.md for the measurement that would justify it; raising
    # shared_buffers is the cheaper first move.

volumes:
  azir-data:
  pg-data:
